# ITU Lab Template Typst

Output in [example.pdf](example.pdf).

## Usage Instructions

Either directly copy-paste the entire contents of template.typ into a new file, or import it as demonstrated in [`example.typ`](example.typ). The logo image should be placed in the same directory as the template file. The easiest way to get started is through the online [app](https://typst.app/).